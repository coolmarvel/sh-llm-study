---
title: MVP 계획 — 챕터 로드맵
created: 2026-09-16
updated: 2026-09-16
---

# MVP 계획: 챕터 로드맵

브리프(`docs/brief.md`)의 시나리오 1~6 을 마일스톤으로 자른다. 상태: `[ ]` 미착수 · `[~]` 진행 중 · `[x]` 완료 · `[?]` 결정 필요.

한 챕터 = 문서 1개(`docs/book/NN-*.md`) + 노트북 1개(`notebooks/NN-*.ipynb`) + 패키지 모듈(`src/shllm/*.py`) + 테스트.
모든 챕터는 **이 PC 의 CPU 에서 노트북 전체 실행이 10분 이내**로 끝나야 한다 (학습 챕터는 "짧은 학습" 셀과 "긴 학습" 스크립트를 분리).

## M0 — 킥오프 + 뼈대 (며칠 안에 만져볼 수 있는 것)

- [x] 프로젝트 생성, 템플릿, 브리프, ADR-0002
- [x] `uv` 환경 + PyTorch CPU + Jupyter, `ruff`/`pytest` 검증 통과
- [x] `src/shllm/` 패키지 뼈대 + 코퍼스 다운로드 스크립트(`scripts/download_corpus.py`)
- [x] 0장 문서·노트북: 환경 확인, LLM 큰 그림, 자바 개발자를 위한 Python/PyTorch 최소 지식
- [x] `notebooks/` 전체 실행(`nbconvert --execute`) 이 검증 명령에 포함

## M1 — 텍스트를 숫자로 (1~2장)

- [x] 1장 문자 토크나이저 + 바이그램 통계 모델: "다음 글자 확률표"만으로 한글 생성 → 왜 문맥이 필요한지 체감
- [x] 2장 BPE 토크나이저 직접 구현 (`shllm/tokenizer.py`): 유니코드·한글 음절/자모 이슈, 어휘 크기 실험, 저장/로드 — `data/tokenizers/bpe-8192.json`

## M2 — 신경망과 임베딩 (3~4장)

- [ ] 3장 임베딩: 룩업 테이블, 위치 임베딩, 학습된 벡터 시각화 (`shllm/embedding.py`)
- [ ] 4장 신경망 기초: 텐서·autograd·경사하강을 NumPy 로 한 번 재현 → PyTorch 로 MLP 언어모델 (Bengio 2003)

## M3 — 어텐션과 Transformer (5~6장)

- [ ] 5장 셀프 어텐션 밑바닥 구현: Q/K/V, 마스킹, 멀티헤드 (`shllm/attention.py`)
- [ ] 6장 Transformer 블록 → GPT 조립: LayerNorm·잔차·FFN, 파라미터 수 세기 (`shllm/model.py`)

## M4 — 학습과 생성 (7~8장) ← **v1.0 성공 기준이 걸린 마일스톤**

- [ ] 7장 학습 루프: 데이터 로더, AdamW, LR 스케줄, 손실 곡선, 체크포인트, CPU 스레드 최적화 (`shllm/train.py`, JSONL 로그)
- [ ] 8장 텍스트 생성: temperature·top-k·top-p·반복 억제 (`shllm/generate.py`) — **한글 프롬프트로 말이 되는 문단 생성**
- [ ] 긴 학습 스크립트(`scripts/train.py --config configs/*.yaml`)로 수천만 파라미터 모델을 수 시간 학습

## M5 — 평가와 그 다음 (9~10장) + 교재

- [ ] 9장 평가: perplexity, 모델 크기 vs 손실 비교(스케일링 맛보기), 어휘 크기 비교
- [ ] 10장 그 다음 지도: 파인튜닝·RLHF·RAG·공개 모델 — 개념만, 구현 없음
- [x] `docs/book/` → PDF 한 권 빌드(`scripts/build_book.py`, weasyprint) → 바탕화면 전달 — 1장 시점에 앞당겨 구현, 챕터마다 재빌드

## M6 — 웹 대시보드

- [ ] oh-my-design 설치 + `/omd:init Linear` → `DESIGN.md`
- [ ] FastAPI: 실험 목록·손실 로그·체크포인트 로드·생성 API·어텐션 맵 API (`dashboard/api/`)
- [ ] React(Vite·TS·Tailwind): 손실 곡선, 어텐션 히트맵, 토큰별 확률 생성 뷰 (`dashboard/web/`)
- [ ] 도커 컴포즈로 8082 포트, Playwright 데스크톱+모바일 뷰포트 검증
- [x] Docker Desktop WSL 통합 활성화 (2026-09-16 확인: `docker` 29.7 동작)

## 열린 결정

- [x] 코퍼스 1차: ko.wikisource 퍼블릭 도메인 20편(114만 자) 으로 확정 (1장까지 충분). 2차 확장은 7장 결과 보고 결정 (todo P2).
